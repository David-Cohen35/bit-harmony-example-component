import React from 'react';
import { Loading } from './loading';

export const ExtraSmallLoading = () => <Loading size="xs" dataTest={''} />;

export const SmallLoading = () => <Loading size="s" dataTest={''} />;

export const MediumLoading = () => <Loading size="m" dataTest={''} />;
